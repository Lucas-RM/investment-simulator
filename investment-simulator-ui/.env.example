# Base URL da API do Simulador de Investimentos.
# Copie para `.env` (não versionado) e ajuste se necessário.
#
# Dev: o Vite usa este valor como target do proxy de `/simular`.
# Produção: o cliente HTTP chama a API neste endereço.
VITE_API_BASE_URL=http://localhost:5001
